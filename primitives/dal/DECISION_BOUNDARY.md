# DAL — Decision Boundary

`NONE`. GenLayer hosts deterministic keyed/2D replay state. Validator/LLM judgment must never determine nonce validity, sequence advancement, replay detection, or authorization identity.
